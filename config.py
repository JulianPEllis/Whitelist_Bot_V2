# If any fields has an apostrophe(') in it, put a backslash(\) in front of it to avoid errors in the code.
# For Example, see line 12.

TOKEN = 'BOT_TOKEN_HERE'

whitelistedUsers = ['Mashhhyyy#7521', 'Example#1234', 'WhitelistedUser#1234'] # Add users to the whitelist by adding their name and tag to this list.

notifyOnKick = True # Would you like to be notified when the bot kicks a user?

notifyID = '' # Place the ID of the Channel/User you want to be notified.

notifyMessage = '{user} was kicked from the server as they aren\'t on the whitelist.'